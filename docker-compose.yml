services:
  app:
    build: .
    ports:
      - "8081:8080"
    depends_on:
      - redis
    env_file:
      - .env
    environment:
      - TZ=Asia/Seoul

  redis:
    image: redis:latest
    container_name: redis